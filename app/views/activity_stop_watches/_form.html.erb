<%= form_with(model: [project, activity_stop_watch], local: true) do |form| %>
  <%= form.hidden_field :description, value: t(:default_stopwatch_description) %>
  <%= form.submit t(:start_stopwatch), class: "btn btn-primary text-lg" %>
<% end %>
