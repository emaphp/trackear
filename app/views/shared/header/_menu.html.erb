<nav class="bg-gray-900">
  <div class="mx-auto container px-4 flex items-center justify-between h-16">
    <a class="navbar-brand" href="/">
      <span class="text-white font-bold">trackear</span>
    </a>
    <div class="flex items-center justify-between text-white">
      <a class="px-3 mx-2" href="/">
        <i class="fas fa-home mr-2"></i>
        <%= t :home %>
      </a>
      <%= link_to(new_project_path, class: "px-3 mx-2") do %>
        <i class="far fa-plus-square mr-2"></i>
        <%= t :create_new_project %>
      <% end %>
      <% if can? :manage, Expense %>
        <%= link_to(expenses_path, class: "px-3 mx-2") do %>
          <i class="fas fa-wallet mr-2"></i>
          <%= t :expenses %>
        <% end %>
      <% end %>
      <% if can? :manage, User %>
        <%= link_to(users_path, class: "px-3 mx-2") do %>
          <i class="fas fa-users mr-2"></i>
          <%= t :users %>
        <% end %>
      <% end %>
      <% if session[:impersonating_from].present? %>
        <%= link_to(destroy_user_session_path, method: :delete, class: "px-3 mx-2") do %>
          <i class="fas fa-sign-out-alt mr-2"></i>
          <%= t :exit_impersionate, email: current_user.email %>
        <% end %>
      <% else %>
        <%= link_to(destroy_user_session_path, method: :delete, class: "px-3 mx-2") do %>
          <i class="fas fa-sign-out-alt mr-2"></i>
          <%= t :logout, email: current_user.first_name %>
        <% end %>
      <% end %>
    </div>
    <%= form_with(model: current_user, url: user_update_locale_url, method: :post, local: true, class: "form-inline my-2 my-lg-0") do |form| %>
      <%= form.select :locale, [['🇦🇷 Español', 'es'], ['🇺🇸 English', 'en']], {}, onchange: "this.form.submit()", class: "px-3 py-2 rounded bg-white" %>
    <% end %>
  </div>
</nav>
